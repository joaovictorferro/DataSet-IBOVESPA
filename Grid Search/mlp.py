# -*- coding: utf-8 -*-
"""MLP.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1z848L7x6Zii8psCqBlnkchm76ehGLnI7

# Imports
"""

from sklearn.model_selection import GridSearchCV
from sklearn.svm import SVR
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split
from sklearn.metrics import mean_squared_error
from sklearn.neural_network import MLPRegressor

import pandas as pd
import numpy as np

df = pd.read_csv("https://raw.githubusercontent.com/joaovictorferro/DataSet-IBOVESPA/main/DataSet_IBOVESPA_Norma.csv")

"""# Division Train and Test"""

X = df.drop(['Close'],axis=1)
Y = df['Close']

X_train, X_test, y_train, y_test = train_test_split(X, Y, test_size=0.3, random_state=42)

"""# GridSearch MLP"""

params = {
    'hidden_layer_sizes': [(17),(24), (32)],
    'activation': [ 'tanh', 'relu', 'identity', 'logistic'],
    'solver': ['adam'],
    'batch_size': [32],
    'learning_rate': ['constant'],
}

grid_search = GridSearchCV(estimator=MLPRegressor(), param_grid=params, cv=5, verbose=0)


grid_search.fit(X_train, y_train)

print("best hyperparameters:", grid.best_params_)